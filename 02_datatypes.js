"use strict"; //treat all JS code as newer version.

alert("Hello World"); //will show an error here as we are 
// running javascript in  nodejs environment not in
// browser environment.

// Mostly in javascript we avoid semicolons.